# fallback 成功候选沉淀清单

## 筛选口径

- case 必须是 benchmark 正确案例：`correct = true`。
- record 必须来自 fallback：`source = fallback`。
- record 必须已经成功标准化：`status = CODED`。
- 同一 abbreviation + expansion 必须存在 `chosen_concept`。
- 本文件只展示候选，不写入 primary 候选库。

## 汇总

- 候选总数: `0`
- 新候选数: `0`
- primary 中已存在: `0`

## 新候选

- 本轮没有新的 fallback 成功候选。

## 已存在候选

- 无。
