[pytest]
testpaths = tests/unit tests/api tests/integration tests/live
python_files = test_*.py
addopts = -ra -m unit
markers =
    unit: deterministic tests that do not require external services
    integration: tests that require local project services such as Milvus
    live: tests that require live LLM, model, network, or Docker services
